function getCart(){return JSON.parse(localStorage.getItem('phonekit-cart')||'[]')}function setCart(c){localStorage.setItem('phonekit-cart',JSON.stringify(c));updateCartCount()}function addToCart(id){let c=getCart(),x=c.find(i=>i.id===id);x?x.qty++:c.push({id,qty:1});setCart(c)}function updateCartCount(){document.querySelectorAll('.cart-count').forEach(e=>e.textContent=getCart().reduce((n,x)=>n+x.qty,0))}document.addEventListener('click',e=>{let b=e.target.closest('[data-add]');if(b){addToCart(+b.dataset.add);b.textContent='Agregado ✓';setTimeout(()=>b.textContent='Comprar',900)}});document.addEventListener('DOMContentLoaded',updateCartCount);
